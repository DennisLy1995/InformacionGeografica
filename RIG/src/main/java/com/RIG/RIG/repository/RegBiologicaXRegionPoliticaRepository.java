package com.RIG.RIG.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.RIG.RIG.domain.REGB_X_REGP;

public interface RegBiologicaXRegionPoliticaRepository extends JpaRepository<REGB_X_REGP, Integer> {

}
